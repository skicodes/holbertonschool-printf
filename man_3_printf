.TH _PRINTF 3 "October 2025" "Holberton School" "C Library Functions"
.SH NAME
_printf \- produces output according to a format
.SH SYNOPSIS
.B #include "main.h"
.PP
.BI "int _printf(const char *format, ...);"
.SH DESCRIPTION
The
.B _printf
function produces output according to a format string containing ordinary characters and conversion specifiers.

The conversion specifiers are:

.TP
.B %c
Prints a single character.
.TP
.B %s
Prints a string.
.TP
.B %%
Prints a literal percent sign.
.TP
.B %d, %i
Prints signed decimal integers.
.TP
.B %b
Prints the binary representation of an unsigned integer.
.TP
.B %u
Prints unsigned decimal integers.
.TP
.B %o
Prints the octal representation of an unsigned integer.
.TP
.B %x, %X
Prints the hexadecimal representation of an unsigned integer (lowercase for %x, uppercase for %X).

If a format specifier is not recognized,
.B _printf
prints the percent sign followed by the unrecognized character.

.SH RETURN VALUE
On success,
.B _printf
returns the number of characters printed (excluding the null byte).
On failure, it returns \-1.

.SH AUTHOR
Written by Roya and Sarah.

.SH SEE ALSO
printf(3)
